# Copilot Instructions

- For Python code responses, always include type hints.
- For JavaScript (or related) code responses, mention the DaisyUI classes that should be used.

# Extensions
Extensions are separate part of the code that can use some shared functions but are not directly implemented there, so never create frontend code in frontend directory when editing extensions nor hardcoded backend part beside extension dir.